# Songs

Place local audio clips here to have them indexed at server startup.

Supported formats: MP3, M4A, OGG, and WAV, up to 50 MB per file.

Files uploaded through the media library in the host editor are also
stored in this folder automatically. Audio binaries are intentionally
excluded from Git — only this README and `.gitkeep` are tracked.
